# Журнал изменений сайта ZORIN

Здесь фиксируется **каждое** изменение сайта — включая мелкие правки
текста, опечатки, одну добавленную точку. Новые записи сверху.

Формат записи: дата · что изменилось · страница/файл · коммит.

Крупные архитектурные решения (смена подхода, провайдера, структуры) —
отдельно и подробно в `PROGRESS_LOG.md`, здесь только короткой строкой
со ссылкой на них.

---

## 2026-09-08

- **Логотипы партнёров: убраны подписи под Шереметьево и Олимпийской
  сборной.** До этого только у этих двух логотипов была подпись
  («Форма для персонала» / «Парадная форма»), у остальных 6 — нет,
  выглядело как рассинхрон. Теперь у всех 8 — только лого и название,
  без исключений. Файл: `src/components/PartnersAndCases.tsx`. Коммит
  `0d232d2`.

## 2026-09-07

- **Заведён этот журнал** (`ЖУРНАЛ.md`) — первым же коммитом вместе с
  этой записью. По просьбе заказчика теперь фиксируются все правки, а
  не только нетривиальные. Обновлён `HANDOFF.md` (раздел «Как вести
  документацию дальше»): пункт 1 — каждое изменение сайта в этот
  журнал.

- **Документация: правила работы с файлами `zorin-site-edits-*.json`.**
  Прописан порядок применения правок из «Редактор сайта.html» —
  проверять каждую правку на мусор / спорные факты / рассинхрон и
  сообщать заказчику до внесения. Файлы: `HANDOFF.md`,
  `AGENT_START_HERE.md`, `PROGRESS_LOG.md`. Коммит `5704475`.

- **Страница «Контакты»: точка в конце заголовка.** H1 на `/kontakty`:
  «Свяжитесь с производством» → «Свяжитесь с производством.». Файл:
  `src/app/kontakty/page.tsx`. Коммит `5d0b5b2`. Тестовая правка —
  проверяли, что весь цикл (правка → сборка → браузер → пуш →
  автодеплой Vercel → живой сайт) работает. Работает, ~30 секунд.

- **Передача проекта заказчику, настройка машины.** Не изменение сайта,
  подробности — `AGENT_START_HERE.md`, раздел «Сессия 6»: установлены
  Node.js / Git / Python, подключён GitHub (через Git Credential
  Manager, без токена), поставлен Playwright для проверки правок в
  браузере.
